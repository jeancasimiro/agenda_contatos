class Contato {
  id: number
  name: string
  email: string
  tell: string

  constructor(id: number, name: string, email: string, tell: string) {
    this.id = id
    this.name = name
    this.email = email
    this.tell = tell
  }
}

export default Contato
